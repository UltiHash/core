include_directories(${CMAKE_CURRENT_SOURCE_DIR})
include_directories(${CMAKE_CURRENT_BINARY_DIR}/..)
include_directories(${Boost_INCLUDE_DIRS})
add_executable(uh-cluster-2 main.cpp)
target_link_libraries(uh-cluster-2 uh_telemetry uh_utils uh_entrypoint uh_license uh_deduplicator uh_storage uh_config ${Boost_LIBRARIES} ${LMDB_LIBRARY} etcd-cpp-api)
add_subdirectory(recovery)
add_subdirectory(common)
add_subdirectory(config)
add_subdirectory(deduplicator)
add_subdirectory(entrypoint)
add_subdirectory(storage)
